# Development environment configuration
# Copy this to .env.development and fill in your values

# JWT signing secret (same as moshi-server's BETTER_AUTH_SECRET)
BETTER_AUTH_SECRET=your-secret-here

# STT server WebSocket URL
STT_SERVER_URL=ws://localhost:8080/api/asr-streaming
